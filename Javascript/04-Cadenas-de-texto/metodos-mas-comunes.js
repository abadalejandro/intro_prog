let str = 'Hello World';
console.log(str.length);

// Obtener partes de cadenas de caracteres.
// slice, substring, subst
let slice_str = str.slice(0, 10);
console.log(slice_str);

let substring_str = str.slice(0, 10);
console.log(slice_str);


let subst_str = str.substr(0, 10);
console.log(slice_str);


// Reemplazar parte de una cadena de texto
let string = "Hola como como estas?";
string.replace('como', 'donde');

let result = string.replace(/como/g, 'donde');
console.log(result);





